import axios from 'axios';

import buildPaginationQueryOpts from '@/shared/sort/sorts';

import { type ITypeOfOrganization } from '@/shared/model/type-of-organization.model';

const baseApiUrl = 'api/type-of-organizations';

export default class TypeOfOrganizationService {
  public find(id: number): Promise<ITypeOfOrganization> {
    return new Promise<ITypeOfOrganization>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public retrieve(paginationQuery?: any): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl + `?${buildPaginationQueryOpts(paginationQuery)}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public delete(id: number): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public create(entity: ITypeOfOrganization): Promise<ITypeOfOrganization> {
    return new Promise<ITypeOfOrganization>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public update(entity: ITypeOfOrganization): Promise<ITypeOfOrganization> {
    return new Promise<ITypeOfOrganization>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public partialUpdate(entity: ITypeOfOrganization): Promise<ITypeOfOrganization> {
    return new Promise<ITypeOfOrganization>((resolve, reject) => {
      axios
        .patch(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
